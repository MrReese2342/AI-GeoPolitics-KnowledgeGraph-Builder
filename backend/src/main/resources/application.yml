spring:
  application:
    name: backend
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password

ollama:
  base-url: http://localhost:11434
  model: llama3.1

management:
  endpoint:
    health:
      show-details: always
